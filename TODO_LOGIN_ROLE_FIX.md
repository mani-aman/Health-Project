# TODO: Login/Register Patient-Only Portal

- [x] Step 1: Update `frontend/src/pages/Login.jsx` — guard login for patient role only
- [x] Step 2: Update `frontend/src/pages/Register.jsx` — add patient-only note + guard signup for patient role only
- [x] Step 3: Update `backend/controllers/auth.controller.js` — hardcode `role: "patient"` in signup controller
